:root{
  --bg:#0a0a0a; --card:#111; --muted:#8b8b8b; --text:#efefef;
  --gold:#d4af37; --gold-2:#b9931f; --accent:#16a34a;
  --ring:#2a2a2a;
  --radius:16px;
}
*{box-sizing:border-box}
body{margin:0; font-family:ui-sans-serif,system-ui,Segoe UI,Roboto,Arial; color:var(--text); background:linear-gradient(180deg,#000,#0b0b0b);}
a{color:var(--gold); text-decoration:none}
.nav{display:flex; justify-content:space-between; align-items:center; padding:16px 20px; position:sticky; top:0; background:rgba(0,0,0,.6); backdrop-filter:blur(6px); border-bottom:1px solid #111}
.brand{font-weight:800; letter-spacing:.12em; color:var(--gold);}
.links a{margin-left:16px; color:#ddd}
.hero{max-width:900px; margin:72px auto 24px; padding:0 16px; text-align:center}
.hero h1{font-size:clamp(32px,6vw,56px); line-height:1.1; margin:0 0 12px}
.gold{color:var(--gold)}
.sub{color:#cfcfcf; margin:0 auto 24px; max-width:720px}
.card{background:linear-gradient(180deg,#0d0d0d,#0a0a0a); border:1px solid var(--ring); border-radius:var(--radius); padding:18px; display:grid; gap:12px; max-width:640px; margin:0 auto; box-shadow:0 10px 30px rgba(0,0,0,.35)}
.mode{display:flex; gap:8px; justify-content:center; flex-wrap:wrap; margin-bottom:6px}
.pill{border:1px solid #2c2c2c; background:#111; color:#ddd; padding:8px 12px; border-radius:999px; cursor:pointer}
.pill.active{background:var(--gold); color:#000; border-color:var(--gold)}
.pill.disabled{opacity:.4; cursor:not-allowed}
label{display:grid; gap:6px; text-align:left}
input[type="url"], input[type="email"]{
  background:#0e0e0e; border:1px solid #242424; color:#eee; padding:12px 14px;
  border-radius:12px; outline:none;
}
input:focus{border-color:var(--gold); box-shadow:0 0 0 3px rgba(212,175,55,.15)}
.check{display:flex; align-items:center; gap:8px; color:#c9c9c9}
.cta{
  background:linear-gradient(180deg,var(--gold),var(--gold-2));
  color:#000; border:none; border-radius:12px; padding:12px 16px; font-weight:700; cursor:pointer;
  box-shadow:0 6px 16px rgba(212,175,55,.2);
}
.cta:disabled{opacity:.6; cursor:not-allowed}
.note{color:#a7a7a7; font-size:12px}
.alert{border:1px solid #3a3a3a; background:#131313; padding:10px 12px; border-radius:10px}
.hidden{display:none}
.how,.faq{max-width:900px; margin:56px auto; padding:0 16px}
.how h2,.faq h2{color:#f2f2f2}
.foot{padding:40px 16px; border-top:1px solid #141414; text-align:center; color:#9a9a9a}
